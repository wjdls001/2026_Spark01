# SPARK 확정 요구사항 명세서

> 작성일: 2026-06-16  
> 기준 문서: spec-origin.md, CLAUDE.md  
> 상태: MVP 구현 대상 확정 요구사항

---

## REQ-01. 인증 — 이메일 회원가입 및 로그인

**우선순위:** P0  
**대상 화면:** `/login`, `/signup`

### 요구사항

- 사용자는 이메일과 비밀번호로 회원가입할 수 있다.
- 사용자는 이메일과 비밀번호로 로그인할 수 있다.
- Supabase Auth를 통해 인증 처리하며, 클라이언트에는 anon key만 사용한다.
- 로그인하지 않은 사용자가 보호된 경로에 접근하면 `/login`으로 리다이렉트한다.
- 로그인 성공 후 온보딩 완료 여부를 확인하고, 미완료 시 온보딩 플로우로 이동한다.
- 로그인 성공 후 온보딩이 완료된 사용자는 `/home`으로 이동한다.

### 완료 기준

- [ ] Supabase Auth 이메일 회원가입 동작
- [ ] 로그인/로그아웃 정상 작동
- [ ] 미인증 접근 시 `/login` 리다이렉트
- [ ] 인증 상태가 새로고침 후에도 유지됨

---

## REQ-02. 온보딩 — 약관 동의 및 프로필 초기 설정

**우선순위:** P0  
**대상 화면:** `/onboarding/terms`, `/onboarding/profile`

### 요구사항

- 신규 회원은 회원가입 직후 약관 동의 화면으로 이동한다.
- 약관 동의 항목: 서비스 이용약관(필수), 개인정보 처리방침(필수), 위치정보 이용약관(선택), 마케팅 수신 동의(선택).
- 필수 약관에 동의하지 않으면 다음 단계로 진행할 수 없다.
- 약관 동의 이력은 `terms_agreements` 테이블에 저장한다.
- 약관 동의 완료 후 프로필 설정 화면으로 이동한다.
- 프로필 설정에서 닉네임(필수), 선호 운동(필수), 운동 레벨(필수), 활동 지역(필수)을 입력받는다.
- 닉네임은 중복 확인을 거쳐야 하며, 중복 시 즉시 안내 메시지를 표시한다.
- 성별, 출생연도, 운동 성향 태그, 프로필 이미지는 선택 입력이다.
- 프로필 저장 완료 시 `profiles` 테이블에 데이터를 저장하고 `/home`으로 이동한다.

### 완료 기준

- [ ] 필수 약관 미동의 시 다음 단계 진행 불가
- [ ] `terms_agreements` 저장 확인
- [ ] 닉네임 중복 확인 동작
- [ ] `profiles` 생성 후 홈 진입 확인

---

## REQ-03. 홈 — 운동 시작 허브 대시보드

**우선순위:** P0  
**대상 화면:** `/home`

### 요구사항

- 홈 화면 최상단 또는 가장 눈에 띄는 영역에 `운동 시작하기` CTA 버튼을 배치한다.
- 로그인한 사용자의 닉네임을 화면 상단에 표시한다.
- 오늘의 운동 상태(완료 여부, 운동 시간 합계)를 요약하여 표시한다.
- 주변 추천 번개 모임을 최대 3개 표시한다. 번개가 없으면 빈 상태 메시지와 번개 생성 유도 링크를 표시한다.
- 진행 중인 챌린지의 달성률을 요약 카드로 표시한다.
- 최근 운동 기록 1~3개를 표시한다.
- 읽지 않은 알림이 있으면 알림 아이콘에 뱃지를 표시한다.

### 완료 기준

- [ ] `운동 시작하기` 버튼이 화면 최상단에 가장 크게 노출됨
- [ ] 오늘의 운동 요약 데이터 표시
- [ ] 추천 번개 리스트 또는 빈 상태 표시
- [ ] 챌린지 달성률 요약 표시

---

## REQ-04. 운동 시작 — 혼자 운동 플로우

**우선순위:** P0  
**대상 화면:** `/exercise`, `/exercise/setup`, `/exercise/session`, `/exercise/result`

### 요구사항

- 운동 시작 화면에서 `혼자 운동`과 `같이 운동` 중 선택할 수 있다.
- 혼자 운동 선택 시 종목 선택 → 목표 설정 → 운동 세션 순서로 진행한다.
- 종목은 `sports` 테이블에서 불러오며, 사용자가 하나를 선택한다.
- 목표 설정 항목: 목표 시간(필수), 목표 거리(선택), 목표 칼로리(선택).
- 운동 세션 화면에서 경과 시간을 타이머로 표시한다.
- 운동 종료 시 `exercise_sessions` 테이블에 기록을 저장한다. mode는 `solo`.
- 운동 결과 화면에서 운동 시간, 거리(입력한 경우), 칼로리(입력한 경우), 획득 XP를 표시한다.
- MVP에서는 GPS 트래킹 없이 타이머 기반으로 구현하며, 거리·칼로리는 직접 입력 또는 mock 처리한다.

### 완료 기준

- [ ] 종목 선택 → 목표 설정 → 타이머 세션 플로우 연결
- [ ] 운동 종료 후 `exercise_sessions` 저장 확인
- [ ] 결과 화면에서 운동 데이터 표시

---

## REQ-05. 운동 시작 — 같이 운동 분기 플로우

**우선순위:** P0  
**대상 화면:** `/exercise`, `/sparks`

### 요구사항

- 같이 운동 선택 시, 현재 승인된 참여 번개가 있는지 확인한다.
- 승인된 번개가 있으면 해당 번개 목록을 보여주고 선택하게 한다.
- 승인된 번개가 없으면 `번개 탐색` 또는 `번개 만들기` 중 선택하게 한다.
- 번개를 통한 운동 완료 시 `exercise_sessions`의 mode는 `spark`, spark_id를 함께 저장한다.

### 완료 기준

- [ ] 참여 번개 유무에 따른 분기 동작
- [ ] 번개 선택 후 운동 세션 연결
- [ ] mode=spark, spark_id 저장 확인

---

## REQ-06. 번개 모임 — 탐색 (리스트 및 지도)

**우선순위:** P1  
**대상 화면:** `/sparks`, `/sparks/map`

### 요구사항

- 번개 모임 탭 진입 시 기본은 리스트 보기이며, 지도 보기로 전환할 수 있다.
- 리스트에는 번개 제목, 운동 종목, 장소명, 예정 시간, 현재 참여 인원/정원, 모집 상태를 표시한다.
- 필터 항목: 운동 종목, 날짜/시간, 거리(근처순). 필터는 복수 적용 가능하다.
- 모집 상태가 `recruiting`인 번개만 기본으로 표시한다.
- 지도 보기는 MVP에서 placeholder 또는 정적 지도로 구현 가능하다.
- 리스트가 비어있을 때 "근처에 열린 번개가 아직 없어요. 직접 만들어볼까요?" 메시지와 번개 생성 버튼을 표시한다.

### 완료 기준

- [ ] 번개 리스트 표시 및 필터 동작
- [ ] recruiting 상태 번개만 기본 노출
- [ ] 리스트/지도 보기 전환 UI
- [ ] 빈 상태 메시지 표시

---

## REQ-07. 번개 모임 — 상세 조회 및 참여 신청

**우선순위:** P1  
**대상 화면:** `/sparks/:sparkId`

### 요구사항

- 번개 상세 화면에서 제목, 운동 종목, 장소, 날짜/시간, 정원, 현재 참여 인원, 호스트 닉네임·운동 레벨·신뢰 점수, 참여 조건(레벨, 성별, 연령), 상세 설명을 표시한다.
- 참여 신청 버튼을 누르면 다음 조건을 순서대로 검증한다.
  1. 로그인 여부 확인 (미로그인 시 `/login` 이동)
  2. 모집 상태가 `recruiting`인지 확인
  3. 정원 초과 여부 확인
  4. 이미 신청 또는 참여한 번개인지 확인
  5. 운동 레벨 조건 충족 여부 확인
- 신청 성공 시 `spark_participants`에 status=`requested`로 저장하고, "신청이 완료되었어요. 호스트 승인을 기다려주세요." 메시지를 표시한다.
- 이미 신청한 번개인 경우 참여 신청 버튼 대신 "승인 대기중" 상태를 표시한다.
- 이미 승인된 번개인 경우 "참여 확정" 상태를 표시한다.

### 완료 기준

- [ ] 번개 상세 정보 표시
- [ ] 5가지 참여 조건 검증 동작
- [ ] `spark_participants` 신청 저장 확인
- [ ] 신청 상태에 따른 버튼 상태 변화

---

## REQ-08. 번개 모임 — 생성

**우선순위:** P1  
**대상 화면:** `/sparks/new`

### 요구사항

- 번개 생성 폼 필수 입력 항목: 운동 종목, 제목, 장소명, 날짜/시간, 정원(최소 2명).
- 번개 생성 폼 선택 입력 항목: 상세 설명, 최소/최대 운동 레벨 조건, 성별 조건, 연령 조건, 소요 시간.
- 번개 생성 완료 시 `sparks` 테이블에 저장하고, 호스트 본인은 `spark_participants`에 role=`host`, status=`approved`로 자동 등록한다.
- 번개 생성 후 생성된 번개 상세 페이지(`/sparks/:sparkId`)로 이동한다.
- 날짜/시간은 현재 시각 이후만 선택 가능하다.

### 완료 기준

- [ ] 필수 항목 미입력 시 유효성 검증 메시지 표시
- [ ] `sparks` 저장 및 호스트 자동 등록 확인
- [ ] 생성 후 상세 페이지 이동

---

## REQ-09. 번개 모임 — 호스트 운영 (승인/거절/마감/종료)

**우선순위:** P1  
**대상 화면:** `/sparks/:sparkId/manage`

### 요구사항

- 호스트는 `requested` 상태의 참여 신청을 승인 또는 거절할 수 있다.
- 승인 시 `spark_participants.status`를 `approved`로 변경한다.
- 거절 시 `spark_participants.status`를 `rejected`로 변경한다.
- 정원이 다 찼을 때 호스트는 모집 마감 처리를 할 수 있다. 상태를 `closed`로 변경한다.
- 호스트는 번개 종료 처리를 할 수 있다. 상태를 `completed`로 변경한다.
- 호스트만 관리 페이지에 접근할 수 있으며, 비호스트 접근 시 상세 페이지로 리다이렉트한다.

### 완료 기준

- [ ] 승인/거절 후 `spark_participants.status` 변경 확인
- [ ] 모집 마감 후 `sparks.status=closed` 변경 확인
- [ ] 번개 종료 후 `sparks.status=completed` 변경 확인
- [ ] 비호스트 접근 차단

---

## REQ-10. 챌린지 — 목록 및 달성률 표시

**우선순위:** P2  
**대상 화면:** `/challenges`, `/challenges/:challengeId`

### 요구사항

- 챌린지 탭은 `운동`, `모임`, `이벤트` 탭으로 구분하여 표시한다.
- 각 탭에는 해당 `challenge_type`의 챌린지 카드를 표시한다.
- 챌린지 카드에는 제목, 목표 횟수, 현재 달성 횟수, 달성률 프로그레스 바, 보상 XP를 표시한다.
- 완료된 챌린지는 "달성 완료" 상태로 표시하고 보상 수령 가능 여부를 안내한다.
- 달성률은 `challenge_progress.current_count / challenges.goal_count * 100`으로 계산한다.
- 챌린지 데이터가 없을 때 "진행 중인 챌린지가 없어요." 빈 상태를 표시한다.

### 완료 기준

- [ ] 3개 탭 전환 및 타입별 챌린지 표시
- [ ] 달성률 계산 및 프로그레스 바 표시
- [ ] 완료 상태 표시

---

## REQ-11. 마이페이지 — 프로필 조회 및 수정

**우선순위:** P1  
**대상 화면:** `/mypage`, `/mypage/profile`

### 요구사항

- 마이페이지에서 닉네임, 프로필 이미지, 운동 레벨, 신뢰 점수, 선호 운동, 운동 성향 태그를 표시한다.
- 프로필 편집에서 닉네임, 선호 운동, 운동 성향, 프로필 이미지를 수정할 수 있다.
- 닉네임 수정 시 중복 확인을 거쳐야 한다.
- 프로필 이미지는 Supabase Storage에 업로드하고 `profiles.avatar_url`에 저장한다.
- 저장 완료 시 변경 사항이 즉시 화면에 반영된다.

### 완료 기준

- [ ] 프로필 정보 표시
- [ ] 닉네임/선호 운동/성향 수정 동작
- [ ] 이미지 업로드 후 즉시 반영

---

## REQ-12. 마이페이지 — 운동 기록 히스토리

**우선순위:** P1  
**대상 화면:** `/mypage/exercise`

### 요구사항

- 자신의 `exercise_sessions` 기록을 최신순으로 표시한다.
- 각 기록 카드에는 운동 종목, 날짜, 운동 시간, 모드(혼자/번개), 상태를 표시한다.
- 주간/월간 운동 통계 요약(총 운동 횟수, 총 운동 시간)을 상단에 표시한다.
- 기록이 없을 때 "첫 운동 기록을 만들어보세요." 빈 상태를 표시한다.

### 완료 기준

- [ ] 운동 기록 리스트 표시
- [ ] 주간/월간 통계 요약 표시
- [ ] 빈 상태 메시지 표시

---

## REQ-13. 마이페이지 — 번개 활동 관리

**우선순위:** P1  
**대상 화면:** `/mypage/activity`

### 요구사항

- 참여한 번개와 개설한 번개 탭을 분리하여 표시한다.
- 각 번개 카드에는 제목, 운동 종목, 날짜, 상태(모집중/마감/진행중/완료/취소)를 표시한다.
- 상태 필터(전체/모집중/완료)를 제공한다.
- 참여한 번개에서 `requested` 상태인 경우 참여 취소 버튼을 제공한다.
- 개설한 번개에서 `recruiting` 상태인 경우 관리 페이지 링크를 제공한다.

### 완료 기준

- [ ] 참여/개설 탭 분리 표시
- [ ] 상태 필터 동작
- [ ] 참여 취소 동작

---

## REQ-14. 공통 — 하단 내비게이션 및 레이아웃

**우선순위:** P0  
**대상:** 전체 인증 화면

### 요구사항

- 인증된 사용자에게는 하단 탭 내비게이션을 항상 표시한다.
- 탭 구성: 홈(`/home`), 운동 시작(`/exercise`), 번개 모임(`/sparks`), 챌린지(`/challenges`), 마이페이지(`/mypage`).
- 현재 활성 탭은 시각적으로 구분한다(색상, 아이콘 강조).
- 온보딩, 로그인, 회원가입, 운동 세션 진행 중(`/exercise/session`) 화면에서는 하단 탭을 숨긴다.
- 기본 화면 폭은 390px 기준으로 설계하며, 데스크탑에서는 중앙 정렬 컨테이너로 표시한다.

### 완료 기준

- [ ] 5개 탭 내비게이션 표시 및 라우팅 동작
- [ ] 온보딩/세션 화면에서 하단 탭 미표시
- [ ] 390px 기준 모바일 레이아웃 깨짐 없음

---

## REQ-15. 공통 — 로딩/에러/빈 상태 처리

**우선순위:** P0  
**대상:** 전체 데이터 조회 화면

### 요구사항

- 데이터를 불러오는 모든 화면에서 로딩 상태를 시각적으로 표시한다(스켈레톤 또는 스피너).
- 네트워크 오류 또는 서버 오류 발생 시 에러 메시지와 재시도 버튼을 표시한다.
- 데이터가 없는 경우 기능에 맞는 빈 상태 메시지와 행동 유도 버튼을 표시한다.
- 표준 빈 상태 메시지:
  - 번개 없음: "근처에 열린 번개가 아직 없어요. 직접 만들어볼까요?"
  - 운동 기록 없음: "첫 운동 기록을 만들어보세요."
  - 챌린지 없음: "진행 중인 챌린지가 없어요."
  - 참여한 번개 없음: "아직 참여한 번개가 없어요."

### 완료 기준

- [ ] 모든 주요 화면에 로딩 상태 구현
- [ ] 에러 발생 시 에러 UI 및 재시도 버튼 표시
- [ ] 각 화면에 맞는 빈 상태 메시지 표시

---

## REQ-16. 보안 — RLS 및 접근 제어

**우선순위:** P0  
**대상:** 전체 Supabase 데이터 접근

### 요구사항

- 모든 public 스키마 테이블에 RLS를 활성화한다.
- 클라이언트 코드에서 service role key를 사용하지 않는다. anon key만 사용한다.
- 사용자는 자신의 `exercise_sessions`, `notifications`, `challenge_progress`, `device_connections`, `terms_agreements` 데이터만 조회/수정할 수 있다.
- `sparks` 수정/삭제는 `host_id`가 현재 사용자인 경우에만 허용한다.
- `spark_participants` 승인/거절은 해당 spark의 `host_id`인 사용자만 처리할 수 있다.
- 신고(`reports`) 내용은 신고자 본인만 조회할 수 있다.

### 완료 기준

- [ ] 타인의 개인 데이터에 접근 불가 확인
- [ ] 비호스트의 번개 수정 불가 확인
- [ ] service role key 프론트 코드에 미노출 확인

---

## 변경 이력

| 날짜 | 버전 | 내용 |
|------|------|------|
| 2026-06-16 | v1.0 | 최초 작성, MVP 기준 16개 요구사항 확정 |
